{% extends 'base.html.twig' %}

{% block title %}Hello JugarController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-4 mt-5 text-center">
                <div class="card-header">
                <h1>{{  url.0.descripcion | capitalize}}</h1>
                </div>
                <div class="card-body">
                    <img src="{{ asset(url.0.imagenUrl) }}" style="height: 400px;width: 400px">
                </div>

                <h2 class="mt-5">Palabras Relacionadas</h2>
                <ul id="lista de palabras">

                </ul>
                <form action="" method="post">

                    <label for="respuesta" class="form-label float-start"> Ingresar Palabras</label>
                    <input type="text" name="respuesta" class="form-control input-group">
                    <button type="submit" class="input-group-text"> Enviar</button>


{#                    <div class="input-group mt-3">#}
{#                        <div class="input-group-text bg-primary"><img src="{{ asset('assets/password-icon.svg') }}"#}
{#                                                                      alt="password-icon" style="height: 1rem"></div>#}
{#                        <input type="password" name="password" id="inputPassword" class="form-control bg-light"#}
{#                               autocomplete="current-password" required placeholder="Contraseña">#}
{#                    </div>#}
                </form>
            </div>

            <div class="col-md-8">
                <div class="table-responsive text-center mt-5">
                <table class="table table-striped table-hover">
                    <thead>
                    <tr>
                        <th>Posición</th>
                        <th>Usuario</th>
                        <th>Puntuación</th>
                    </tr>
                    </thead>
                    <tbody>
                    aquí se mostrarán las puntuaciones de los usuarios
                    </tbody>
                </table>
            </div>
            </div>
        </div>
    </div>
{% endblock %}
