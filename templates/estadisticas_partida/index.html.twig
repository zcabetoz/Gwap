{% extends 'base.html.twig' %}

{% block title %}Estadisticas | {{ parent() }} {% endblock %}

{% block body %}
    <div class="container container-fluid">
        <div class="row justify-content-center">
            <div class="col-md-12">
                <div id="div-estadisticas" class="card-header">
                    <i class="bi bi-joystick"><span style="margin-left: 5px">Gwap</span></i>
                    <span style="margin-left: 100px">Estadisticas de la partida</span>
                    <a href="{{ path('app_dashboard') }}" class="volver">Menu Principal</a>
                </div>
            </div>
        </div>
        <section style="margin-left: 50px;color: white">
            <div class="row mt-3">
                <div class="col-md-4">
                    <div class="card-body">
                        <h2>{{ jugadores.0.nombre_jugador }}</h2>
                        <div class="table-responsive text-center mt-3">
                            <table class="table table-striped table-hover">
                                <thead style="color: white; font-size: 20px">
                                <tr>
                                    <th>Imagen</th>
                                    <th>Palabra Relacionada</th>
                                    <th>Resultado</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for palabras in jugador_1 %}
                                    <tr>
                                        <td class="text-white">
                                            {{ palabras.id_imagen }}
                                        </td>
                                        <td class="text-white">
                                            {{ palabras.palabras_relacionadas }}
                                        </td>
                                        <td class="text-white">
                                            {{ palabras.resultado_palabra }}
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                {#        </div> #}
                {#        <div class="row mt-3"> #}
                <div class="col-md-4">
                    <div class="card-body">
                        <h2>{{ jugadores.1.nombre_jugador }}</h2>
                        <div class="table-responsive text-center mt-3">
                            <table class="table table-striped table-hover">
                                <thead style="color: white; font-size: 20px">
                                <tr>
                                    <th>Imagen</th>
                                    <th>Palabra Relacionada</th>
                                    <th>Resultado</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for palabras in jugador_2 %}
                                    <tr>
                                        <td class="text-white">
                                            {{ palabras.id_imagen }}
                                        </td>
                                        <td class="text-white">
                                            {{ palabras.palabras_relacionadas }}
                                        </td>
                                        <td class="text-white">
                                            {{ palabras.resultado_palabra }}
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                {#        </div> #}
                {#        <div class="row mt-3"> #}
                <div class="col-md-4">
                    <div class="card-body">
                        <h2>{{ jugadores.2.nombre_jugador }}</h2>
                        <div class="table-responsive text-center mt-3">
                            <table class="table table-striped table-hover">
                                <thead style="color: white; font-size: 20px">
                                <tr>
                                    <th>Imagen</th>
                                    <th>Palabra Relacionada</th>
                                    <th>Resultado</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for palabras in jugador_3 %}
                                    <tr>
                                        <td class="text-white">
                                            {{ palabras.id_imagen }}
                                        </td>
                                        <td class="text-white">
                                            {{ palabras.palabras_relacionadas }}
                                        </td>
                                        <td class="text-white">
                                            {{ palabras.resultado_palabra }}
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
